# ytptube/.env.example
# YTPTube has no secrets managed via .env. All configuration is done through
# the web UI after first launch.
#
# No .env file is required for this service.
# Update volume paths and user directly in compose.yaml.
#
# Web UI: http://your-host:8081
