# audiobookshelf/.env.example
# Audiobookshelf has no secrets — all configuration is done via environment variables
# in compose.yaml or through the web UI after first launch.
#
# No .env file is required for this service.
# Update the TZ, PUID, PGID, and volume paths directly in compose.yaml.
