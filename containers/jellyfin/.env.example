# jellyfin/.env.example
# Jellyfin has no secrets managed via .env. All configuration is done through
# the web UI on first launch (admin account, media library paths, transcoding settings).
#
# No .env file is required for this service.
# Update TZ and volume paths directly in compose.yaml.
#
# Web UI: http://your-host:8096
