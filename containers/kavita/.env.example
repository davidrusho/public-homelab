# kavita/.env.example
# Kavita has no secrets managed via .env — all configuration is done through
# the web UI after first launch (admin account creation, library paths, OPDS, etc.)
#
# No .env file is required for this service.
# Update the TZ, PUID, PGID, and volume paths directly in compose.yaml.
